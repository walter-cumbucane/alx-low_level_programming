#include "main.h"

/**
 * _memcpy - set the integer to 402
 * @src: a pointer the integer we want to set to 98
 * @dest: letter being tested
 * @n: letter being tested
 * Return: nothing
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	return (NULL);
}
