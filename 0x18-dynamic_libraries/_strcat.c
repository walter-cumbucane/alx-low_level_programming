#include "main.h"

/**
 * _strcat - set the integer to 402
 * @dest: a pointer the integer we want to set to 98
 * @src: the string to be concatenated
 * Return: nothing
 */

char *_strcat(char *dest, char *src)
{
	return (NULL);
}
