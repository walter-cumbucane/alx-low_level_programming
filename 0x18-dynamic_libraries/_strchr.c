#include "main.h"

/**
 * _strchr - set the integer to 402
 * @s: a pointer the integer we want to set to 98
 * @c: letter being tested
 * Return: nothing
 */

char *_strchr(char *s, char c)
{
	return (NULL);
}
