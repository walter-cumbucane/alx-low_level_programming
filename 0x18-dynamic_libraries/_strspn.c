#include "main.h"

/**
 * _strspn - set the integer to 402
 * @s: a pointer the integer we want to set to 98
 * @accept: letter being tested
 * Return: nothing
 */

unsigned int _strspn(char *s, char *accept)
{
	return (0);
}
