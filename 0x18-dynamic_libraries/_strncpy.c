#include "main.h"

/**
 * _strncpy - set the integer to 402
 * @dest: a pointer the integer we want to set to 98
 * @src : letter being tested
 * @n : letter being tested
 * Return: nothing
 */

char *_strncpy(char *dest, char *src, int n)
{
	return (NULL);
}
