#include "main.h"

/**
 * _strncat - set the integer to 402
 * @dest: a pointer the integer we want to set to 98
 * @src: the string to be concatenated
 * @n : letter being tested
 * Return: nothing
 */

char *_strncat(char *dest, char *src, int n)
{
	return (NULL);
}
