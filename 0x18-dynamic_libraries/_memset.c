#include "main.h"

/**
 * _memset - set the integer to 402
 * @s: a pointer the integer we want to set to 98
 * @b: letter being tested
 * @n: letter being tested
 * Return: nothing
 */

char *_memset(char *s, char b, unsigned int n)
{
	return (NULL);
}
